# Resume Builder Recovery Guidance

When a session resumes, inspect app state before continuing.

If a recovery draft exists, show the owner what was saved and ask whether to keep or discard it. Do not overwrite a recovery draft silently. If state changed during a paused session, prefer the latest Resume-domain state and ask one concise question when owner intent is ambiguous.
