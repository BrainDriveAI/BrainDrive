# First-party app contract foundation

This directory is the executable contract boundary shared by the BrainDrive app platform and reviewed installed apps. Resume Builder and Brief Builder are independently developed apps, not BrainDrive first-party product modules. These contracts provide generic authority and transport; app-specific workflow and inference policy stays in each signed app package. Importing these modules alone does not register a route, start a process, create storage, call a provider, or enable an app.

The generated model-compatibility entry schema reads legacy v1 records and strict v2 evidence. V2 retains every content-free run outcome and binds exact identity/configuration/policy/schema/corpus, three unique operations per fixture, decomposed success/recovery/failure counts, evidence class, validity, p95 latency, and an exact 90-day expiry. Legacy records remain readable but are classified by the runtime as provisional rather than release evidence.

## Generic first-party registration

`app-registry.ts` defines canonical lower-case app, publisher, route, capability, and inference-purpose identifiers; safe catalog presentation; manifest revision 2; verified package joins; independently reviewed capability/purpose/data-adapter registrations; and the immutable resolved descriptor. `../registry.ts` builds exact app-ID and route-key indexes without reading packages, owner data, or runtime state. Construction is deterministic and side-effect free. Duplicate or folded identities, unsafe metadata, package/registry identity disagreement, and missing reviewed bindings fail closed.

A manifest declares package files and requests authority. It cannot name a host handler, module/import, inference policy, or data adapter. The host registration uses bounded opaque binding IDs that identify reviewed compiled composition; it does not load a manifest-provided path. Generic capability and purpose identifiers are bounded syntax, not grants: an app request becomes resolvable only when the host registration contains the exact app-scoped name and version.

`package-components.ts` adds the SC-001 package/component conformance contract for the generic sidecar runtime. It is additive and non-executing: it validates packages that contain launchable app components, capability-provider components, combined app/provider components, dependency-service components, app-owned sidecars, provider sidecars, provided operations, capability dependencies, target matrices, configuration, vault-backed secret declarations, permissions, lifecycle policy, diagnostics policy, retention policy, and evidence staleness policy. It rejects public sidecar binds, unmanaged secret delivery, unsupported runtime targets, missing operation contracts, silent provider install/switch policy, and manifest fields that try to select Host handlers, imports, private bindings, endpoints, ports, raw provider responses, host paths, credentials, or internal service names. Importing the contract or passing conformance does not install a package, start a sidecar, route an operation, show an Apps card, generate Docker services, or qualify desktop runtime behavior.

`supervisor.ts` now also defines the additive SC-003 sidecar binding projection and lifecycle diagnostic event contracts. These schemas describe only safe Host-owned status: package/component identity, target label, transport class, audience class, generation, lifecycle state, health class, restart attempt, and allowlisted error code. They intentionally omit raw endpoints, local ports, bearer material, process/container IDs, host paths, internal service names, and provider payloads. The private binding service in lifecycle code may hold adapter-scoped runtime authority in memory, but the contract projection is the only owner/consumer-safe shape.

`diagnostics.ts` adds the SC-009 generic package diagnostics contract. It defines content-free operation receipts, sidecar lifecycle projections, qualification-staleness events, the shared retention policy, and support-bundle posture for provider packages. The projection is package/provider/component/operation oriented and bounded to IDs, versions, safe status, typed failure codes, counts, duration, transport class, target label, and evidence classes. It rejects or normalizes unsafe details and does not retain raw provider responses, page content, owner-private content, prompts, credentials, endpoints, local ports, internal network names, process/container IDs, package/host paths, or raw support-bundle payloads. Internet Search diagnostics use this contract as a proof package wrapper rather than owning the generic policy.

Manifest revision 2 may now add an optional `presentations` declaration for generic app workspace contracts. The declaration is strict and non-executing: `AppPresentationProfileSchema` distinguishes existing `surface` profiles from current `chat_workspace` profiles, and `ChatWorkspaceDescriptorSchema` declares only bounded document ids, optional app-declared empty-state copy, digest-bound app resources, purpose-limited context requests, and action descriptors. `primary_resource` remains required for the existing sandboxed surface path, and a surface presentation must bind that same `ui://` resource. App resources must bind declared read-only package files by path and digest. Action descriptors must carry digest-bound, resolved input and result JSON Schema bodies; missing schemas, duplicate schema IDs, digest mismatches, unresolved references, and oversized schema bodies fail validation. Context and action descriptors may reference only capabilities or inference purposes already requested by the manifest; they cannot name handlers, modules, provider credentials, host paths, filesystem destinations, or arbitrary owner-memory traversal. `ResolvedAppDescriptorSchema` projects the optional presentation set for the app-chat session, generic document, model-tool, and UI renderer paths, but these contracts alone do not launch a view or grant app authority.

`app-artifacts.ts` defines the app-neutral artifact and export wire boundary. Artifact registration binds owner/app/install/package authority, operation and idempotency identity, source identity, content digest/size, retention class, safe media type, and owner-visible label. Export preparation accepts only host-confirmed PDF or strict UTF-8 text bytes with a safe filename label; destination intent is declared as owner-visible policy, never as a filesystem path. Export finalization records a replayable receipt and projects only opaque receipt/artifact identities, digest, media type, outcome, and safe destination label. Receipts and errors omit bytes, raw paths, provider credentials, and owner authorization state.

`app-storage.ts` defines the app-neutral durable document/state boundary. App-owned workspace documents are addressed by app-declared document ids and data binding ids, then stored under owner/app/installation authority with package digest, lifecycle generation, grant, revision, revocation, operation, idempotency, role, media type, retention class, content digest, and content size metadata. The current document surface may contain app document content; audit projections are content-free. Create, update, and delete use revision compare-and-swap plus idempotency-keyed canonical input. Deletes tombstone durable owner data, provenance, operation lookup, and recovery-window classes; only disposable preview cache and transient abandoned-operation classes may use physical deletion.

`app-action-plan.ts` defines the optional private runtime planning contract for native app-chat actions. BrainDrive sends the selected action id, concrete app-declared action input, owner-confirmation state, operation/idempotency identity, session identity, and current app-owned document snapshots to the app runtime tool `app.actions.plan` when that tool is available. The app returns a strict ordered plan containing only generic host-executable steps: `capability.call`, `document.read`, `document.write`, and `export.prepare`. The plan schema lets the app own action semantics, document projections, and renderer/export bytes while the host still enforces declared capabilities, current grant authority, document scope, idempotency, owner confirmation, digest/size checks, and content-free audit.

Revision-1 `PackageManifestSchema` is the installed Resume compatibility format. `LegacyResumePackageManifestSchema` and `parseLegacyResumePackageManifestForMigration` make that boundary explicit; the schema accepts only the canonical Resume identity, so another app cannot use it. New generic candidates must use `GenericPackageManifestSchema` revision 2. Milestone 1 does not switch the active package verifier or migrate stored state.

Spec 2's data-specific REQ-001–REQ-040 traceability is recorded separately in `fixtures/spec-02-requirements.json`; `fixtures/requirements.json` remains the accepted cross-spec Resume Builder REQ-001–REQ-034 manifest. `data-conformance.ts` freezes the data-only capability context/request/result, owner-safe state, migration provenance and compatibility, retention, sensitivity, CAS, and transition rules without becoming a runtime route or a second persistence implementation.

Contract authority is the project-owner-approved Resume Builder Specs 1–6 and the accepted Spec 07 quality-gate-correction addendum in `/home/hex/Reference/Designs/BrainDrive-Tools/Resume-Builder/MVP/`. Spec 06, its test plan, and `resume-quality.md` revision 3 remain accepted authority. On 2026-08-11 the product owner accepted the correction addendum, its dedicated verification plan, and the explicit implementation-blocker dispositions for RB7-OQ-1 through RB7-OQ-4.

Stable content-free Resume validator rule IDs are admitted only on deterministic-validation attempt diagnostics. Attempt diagnostics may add only a bounded duration class and, for structural failures, the coherent structural subtype (`empty_output`, `invalid_json`, `purpose_schema_mismatch`, or `host_normalization_mismatch`). Terminal General recovery diagnostics retain separate provider, targeted-repair, and applicable full-constructor rule-ID arrays alongside coherent safe-code and disposition fields; raw findings and owner content remain excluded.

The earlier review-only requirement and contract inventories remain historical records in `fixtures/spec-07/m1-review-manifest.json`, `fixtures/spec-07/m2-review-manifest.json`, and `fixtures/spec-07/m3-review-manifest.json`. The correction addendum does not retroactively turn those manifests, historical reports, or provisional policies into release evidence.

Spec 04's lifecycle-specific REQ-001–REQ-040, G0–G6, transition/failure matrices, and fixture corpus are recorded under `fixtures/spec-04/`. `SPEC-04-M1-VERIFICATION.md` is the accepted lifecycle verification plan and `app-lifecycle.m1.test.ts` is its focused executable audit. Run it with `npm run test -- app-lifecycle` from `builds/typescript`.

Spec 05's REQ-001–REQ-045 traceability, OQ-1–OQ-7 decisions, adversarial corpus, and M1 gate evidence are recorded under `fixtures/spec-05/`. `spec-05-foundation.ts` is the dormant version-1 wire/security authority and `SPEC-05-M1-VERIFICATION.md` records the accepted evidence plan. Run the focused proof with `npm run test -- app-platform/contracts/spec-05-m1.test.ts` from `builds/typescript`.

The Spec 05 modern profile uses the split MCP v2 packages at `2.0.0` and targets stateless MCP `2026-07-28`; the bounded fixed-tool profile keeps the monolithic SDK `1.30.0` at MCP `2025-11-25`. Apps remains `io.modelcontextprotocol/ui` `2026-01-26` through `@modelcontextprotocol/ext-apps` `1.7.5`. These contracts do not connect to a server or expose app authority merely by being imported.

All authority-bearing envelopes are strict: unknown fields fail. Durable owner-data records use an explicit `extensions` object so a compatible reader can preserve unknown extension fields without granting them authority. The active data schema is version 4: current packages read versions 2–4 and write only version 4. Schemas 0 and 1 remain migration inputs through the staged 0→1→2→3→4 startup chain, not current package compatibility claims. Unknown newer versions and destructive downgrade attempts fail closed.

`InterviewTurnAuditSchema` is the one recognized content-bearing version-1 source extension for submitted Resume Builder interview provenance. It requires opaque turn/session identities, prompt version, topic, exact owner-visible question, submitted answer or skip, optional visible follow-up outcome, action, and occurrence time. Cross-field validation rejects answers on skipped turns, missing answers on answered turns, follow-ups on skips, and inconsistent follow-up outcomes. It excludes keystrokes, unsent input, hidden/system prompts, internal reasoning, provider payloads, and credentials. The extension is durable owner data; capability/lifecycle audit contracts remain content-free.

Schema-2-and-newer interview progress carries the exact acknowledged recovery slot/value/time/revision and last submitted turn identity. Schema 3 adds strict coverage, strategy, target-fit, historical craft-report, single-repair, and artifact-parity records. Schema 4 adds the corrected score-free craft-report v2 authority, the seven-value owner quality-state vocabulary, approval evidence v2, Career return metadata v2, and the synthetic frozen-regression manifest. Historical craft/approval and repair-operation v1 records remain readable; historical review derives only `pre_correction_review` and is insufficient for a corrected new approval. New repair-operation v2 records bind one `repair_statement` action, one correction class, exact statement scope, same provider/model, input/output and source/report digests, terminal transition, safe recovery reason, and optional successor identities. An artifact-parity report requires all five unique representations, a disposition consistent with its mismatch categories, and a digest over its complete non-envelope body. Coverage keeps non-fact dispositions separate from career facts; only answered job evidence is writable as a schema-3-or-newer career fact. The bridge `capability.call` may carry a separate `request_operation_id` for semantic retry while `message_id` remains transport replay/correlation identity. The strict recovery lifecycle projection distinguishes unresolved current-process absence from settled restart absence after verified store transaction recovery. Inference terminal diagnostics can distinguish provider validation from ordered targeted/full local recovery using only an all-or-none coherent set of allowlisted failure codes, candidate classes, validator-code arrays, and dispositions. Recovery and persuasive-quality diagnostics otherwise use the same strict content-free event body: only allowlisted IDs, enums, counts, timings, and digests are admitted; owner text, resume/job content, prompts, provider bodies, reviewer transcripts, credentials, and paths remain prohibited.

Package verification authority is split deliberately: `PackageManifestSchema` describes archive identity and contents, `PackageDescriptorSchema` binds canonical manifest and exact archive digests to an Ed25519 signature, `PackageSourceIndexSchema` resolves immutable descriptors, `TrustRootSchema` defines the pinned-root/release-key hierarchy, and `RevocationListSchema` supplies monotonic explicit denials. Current first-party fixtures retain the required Docker Linux x64 and desktop Windows x64 artifacts and may add exactly one `desktop_macos_universal`/`macos`/`universal` packaged-Node artifact with a matching signed `desktop_macos` source. Existing two-target signed packages remain valid; a macOS host fails closed unless the selected package and source index both authorize the macOS target. `supervisor.ts` freezes the runtime-neutral control protocol; it does not implement or start a supervisor.

`lifecycle-foundation.ts` freezes seven separated storage classes, mixed-version fail-closed behavior, and the opaque Spec 02 lifecycle-data adapter. `audit.ts` defines the strict lifecycle diagnostic allowlist. These contract modules expose no downloader, store, process launch, registration, persistence, API, or UI entrypoint.

`audit.ts` also defines the strict content-minimized Spec 10 Resume recovery reconciliation detail contract used by the ordinary JSONL sink and support-bundle projection. Its additive fields are opaque IDs/digests, revision/count/timing classes, idempotency/final/conflict dispositions, and a safe error code; it has no owner value, prompt, endpoint, credential, or path surface.

Spec 10 Milestone 5 adds a separate strict ordinary-audit contract for explicit General Resume retries. Uniquely named fields retain only the `owner_initiated_retry` class, prior/new operation IDs, semantic-input digest, frozen strategy/provider/model identities, and authoritative equivalence result. The support-bundle sanitizer preserves that bounded relation while dropping generic reason fields and all owner content, prompts, provider bodies, endpoints, credentials, and paths.

The generated Spec 10 authorities also include the strict recovery reconciliation query and operation-lifecycle projection. The latter is a content-free host/package read contract and converts directly into the shared reconciliation decision's host-settlement and operation-readback inputs.

The valid trust/source/revocation/package-signature fixtures contain mutually verified public keys and signatures. Their one-time private keys were discarded and are not present in the repository, so later runtime fixture generation must use an ephemeral test trust root or release-authority-provided signing material rather than treating conformance vectors as signing credentials.

JSON Schema artifacts are generated from the Zod authorities with `npm run contracts:schemas` from `builds/typescript`. Generic artifacts use the `app-platform/v1` schema namespace; retained Resume-specific artifacts keep the `resume-builder/v1` namespace. Generated artifacts are checked for drift by the contract tests.

SC-001 conformance evidence lives in `fixtures/sidecar-package/sc-001-conformance-corpus.json` and `package-components.test.ts`. Run it with `npm run test -- app-platform/contracts` from `builds/typescript`, then regenerate schemas with `npm run contracts:schemas` after contract changes.

SC-009 diagnostics evidence lives in `diagnostics.test.ts` and Internet Search wrapper coverage in `../../internet-search/diagnostics.test.ts`. Run `npm run test -- app-platform/contracts/diagnostics.test.ts internet-search/diagnostics.test.ts` from `builds/typescript` for the focused redaction/staleness proof. The broader SC-009 gate is `npm run test -- internet-search/diagnostics.test.ts app-platform app-capabilities` plus lint, build, docs verification, generated-doc sync, and current secret scanning.

Spec 09 adds an additive, content-free terminal inference vocabulary in `inference.ts`. New results and app inference events may carry strict outcome metadata for the normalized stage, finish category, bounded attempt count, retryability, recovery class, completion mode, and final disposition. Previously persisted version-1 results and events without that optional metadata remain readable. Legacy error codes remain parseable, while new outcomes can preserve malformed structure, incomplete output, evidence rejection, provider schema/authentication/authorization failures, filtering/refusal, unexpected tools, cancellation, and internal failure without carrying provider payloads or owner content.

Resume Builder compatibility data currently maps to the repository-consistent `apps/resume-builder` namespace below the configured memory root. These contracts do not initialize or write that namespace. The reviewed Resume adapter still exposes `career.context.read`, `career.facts.read`, `career.facts.propose`, `career.facts.confirm`, `resume.definitions.read`, `resume.definitions.write`, `resume.jobs.read`, `resume.jobs.write`, `resume.artifacts.register`, `resume.export.request`, and `resume.operations.read` as app-scoped capabilities; that inventory is not a generic BrainDrive action vocabulary. Inference remains a separate host capability. The Resume inference allowlist includes `resume_strategy`, version-2 `tailoring_plan`, `resume_craft_evaluate`, and `resume_craft_repair`; the app package owns the prompts, schema, issue IDs, adjudication, and fallbacks while the host enforces provider mediation, no-tools execution, and bounded attempts.
