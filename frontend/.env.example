# Example Environment Configuration for BrainDrive Frontend
# Copy this file to .env.local to customize settings

# API Configuration
VITE_API_URL=http://localhost:8005
VITE_API_TIMEOUT=10000

# Development Only - Temporary Auto Login (Remove in production)
VITE_DEV_AUTO_LOGIN=false
VITE_DEV_EMAIL=none
VITE_DEV_PASSWORD=password

# Note: Do NOT set NODE_ENV in .env files. Vite automatically sets this based on the command:
# - npm run dev → NODE_ENV=development
# - npm run build/preview → NODE_ENV=production